#include <stdio.h>

int main(void) {
    int a, b;

    printf("Upisi dva cijela broja > ");
    scanf("%d %d", &a, &b);

    if ((x < 100 + y) || ((y % 2 != 0) && (y <= 0))) {
        printf("Istina je");
    }
    
    return 0;
}